 import { Center, ChevronDownIcon, Select, SelectTrigger, SelectInput, SelectPortal, SelectBackdrop, SelectContent, SelectDragIndicatorWrapper, SelectDragIndicator, SelectItem, Icon, View, FlatList } from '@gluestack-ui/themed';


import { SelectIcon  } from '@gluestack-ui/themed';
import { useState } from 'react';
import React, { StyleSheet } from 'react-native';
import CustomCard from './CustomCard';
const CustomSelection=({arrayLength})=>{
const [select,isSlelected] = useState(false)
const flatListRef = useRef(null)    
return(
 <></>
    
)
}
const styles=StyleSheet.create({
    container:{
        flexDirection:'column',
        justifyContent:'center',
        width:'100%'
    }
})
export default CustomSelection
