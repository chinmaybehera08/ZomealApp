import React , {useState} from 'react'
import { Card, ScrollView, useTheme } from 'native-base'
import { HStack, VStack, Heading, Box, Text, View, Center, Image, Button } from 'native-base'
import Packages from './Packages'
import TodaysMenu from './TodaysMenu'
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import { NavigationContainer } from '@react-navigation/native'
import TabNaivgator from '../navigation/TabNavigator'




const Tab = createBottomTabNavigator();

const Dashboard = ({navigation}) => {

 
   return (
      <Box>
         
      </Box>
)
}
export default Dashboard
